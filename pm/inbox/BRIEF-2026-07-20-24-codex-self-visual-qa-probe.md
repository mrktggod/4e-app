status: HOLD-MANUAL

# BRIEF-2026-07-20-24-codex-self-visual-qa-probe

## ⚠️ НЕ для ночного автопрогона
Запускать ТОЛЬКО интерактивно при Юрии. Браузер/computer-use может встать на подтверждении → в фоне зависнет до утра. Ночной runner: НЕ бери этот бриф (статус не NEW). Юрий даёт его вручную, когда рядом.

## Context
Проверяем гипотезу: Codex-app сам (через встроенный браузер / computer use) может делать визуальный QA по preview-ссылкам — без отдельного чата ChatGPT и без API-трат. Если сработает — авто-тестировщик после ночной работы. Это ПРОБА возможности под присмотром Юрия.

## Task
1. Проверь, доступен ли тебе браузер / computer use / скриншот-инструмент В ЭТОМ (автоматизационном) режиме. Если НЕТ — честно напиши «browser недоступен в automation», заверши, статус NEED-YURI (тогда останемся на полуавтомате: пакет → GPT вручную). Не выдумывай визуальные выводы без реального просмотра.
2. Если браузер доступен — открой 6 preview-экранов (ссылки из `docs/qa/GPT-QA-PACKET-2026-07-20.md`: dashboard, task-detail, profile, subscription, chats, chat-conv) и визуально проверь: overflow/обрезки, читаемость, налезания, nav не перекрывает контент, бейдж подписки «Выгода 16%» (не «20%»).
3. Классифицируй находки blocker/major/minor в формате из QA-пакета, запиши в `pm/qa-results-2026-07-20.md` (не заводи фиксы сам — это триаж Cowork утром).

## Stop Points
- ❌ Ничего не менять в коде — только смотреть и записать отчёт.
- ❌ Не логиниться, не платить, не трогать реальные аккаунты/секреты/prod.
- ❌ Не тестировать auth/оплату/live-Telegram (вне зоны визуального QA).

## Verification
- Если браузер сработал — приложи, какие экраны реально открыл (доказательство просмотра, не догадки).
- `check-cp1251-mojibake` = 0 (если создаёшь файл отчёта).

## Report
`pm/outbox/REPORT-BRIEF-2026-07-20-24-codex-self-visual-qa-probe.md`: сработал ли браузер в automation (да/нет), список находок (если да), вывод — годится ли Codex-self-QA как авто-тестировщик или нужен полуавтомат/внешний GPT.
