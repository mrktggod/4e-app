status: NEW
priority: P1

# BRIEF-2026-07-30-115 — VK AI-чат: паритет с основным (BACK-066C) (P1)

Делать ПОСЛЕ `BRIEF-2026-07-30-114` (честные ошибки) — правки в одном файле, риск конфликта при параллельной правке. Источник: `docs/tasks/BACK-066-vk-functional-parity-audit-2026-07-22.md`, рекомендованный fix-бриф `BACK-066C`.

### Контекст

Основной AI-чат (`index.html`) хранит удалённую историю, использует профильные факты/память, нормализованные теги `<create_task>`/`<task_actions>`, сохраняет `originalMsg`. VK AI-чат (`vk.html`) хранит только локальный срез истории (20 сообщений), без `originalMsg` и без нормализованных action-тегов.

### Задача

1. `vk.html` AI-чат (~строка 1516) — сохранять `originalMsg` при создании задачи из чата.
2. Добавить обработку нормализованных `<create_task>`/`<task_actions>` тегов, если backend уже их отдаёт универсально для всех поверхностей — не изобретать новый формат на VK.
3. Если backend не отдаёт это универсально — зафиксировать в REPORT как отдельный backend NEED-CLAUDE пункт, не трогать backend автономно.
4. Не расширять до полной памяти/фактов профиля — это SMART-007, отдельный скоуп.

### Стоп-точки

- Не менять backend `/anthropic` контракт без отдельного ревью.
- Без прод-деплоя, без merge в `main`, без live VK-аккаунта.

### Ожидаемый результат

Задача, созданная из VK AI-чата, содержит `originalMsg` и (если backend поддерживает) реагирует на те же action-теги, что Telegram/Web; smoke, сравнивающий поведение на VK и в основном приложении; `check-cp1251-mojibake` pass.

---
