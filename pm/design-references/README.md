# Design references

Put Alexey's glass-card mockup here before the night runner starts.

Preferred filename:

```text
pm/design-references/glass-card-reference.png
```

Accepted alternatives:

```text
pm/design-references/glass-card-reference.jpg
pm/design-references/glass-card-reference.webp
pm/design-references/glass-card-reference.jpeg
```

The night runner must treat the image as the visual target for `DESIGN-GLASS-001`. If no reference image exists, it should stop after inventory/planning and report `NEED-REFERENCE` instead of guessing pixel details.
