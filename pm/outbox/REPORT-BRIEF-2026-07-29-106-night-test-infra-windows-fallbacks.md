# REPORT-BRIEF-2026-07-29-106-night-test-infra-windows-fallbacks

Status: DONE

## Changed files

- `scripts/run-bash-script.mjs`
- `scripts/check-portable-paths.mjs`
- `scripts/check-ui-architecture.mjs`
- `scripts/lib/find-chrome.mjs`
- `scripts/back-055-notifications-smoke.mjs`
- `scripts/back-069-task-detail-hero-overflow-smoke.mjs`
- `pm/inbox/BRIEF-2026-07-29-106-night-test-infra-windows-fallbacks.md`
- `pm/outbox/REPORT-BRIEF-2026-07-29-106-night-test-infra-windows-fallbacks.md`

## Fix

Kept the existing bash guard path for Linux/GitHub Actions and added same-name Node fallbacks for Windows environments where `bash` is missing. The fallback thresholds mirror the shell guards and do not weaken existing limits.

Added a shared CDP browser helper that tries `CHROME_PATH`/`BROWSER_PATH`, common installed Chrome/Edge locations from environment variables, PATH commands, and finally Playwright Chromium. Updated `back055` and `back069` CDP smokes to use that helper.

No app runtime behavior was changed.

## Before

- `npm run check:portable-paths`
  - FAIL: `Unable to run scripts/check-portable-paths.sh: spawnSync bash ENOENT`
- `npm run check:ui-architecture`
  - FAIL: `Unable to run scripts/check-ui-architecture.sh: spawnSync bash ENOENT`

## After

- `npm run check:portable-paths`
  - PASS: `Portable path check passed.`
- `npm run check:ui-architecture`
  - PASS: inline style attributes 284/465, inline event handlers 401/402, style tags 0/0, inline script tags 3/3.
- `npm run smoke:back055`
  - PASS.
- `npm run smoke:back069-hero`
  - PASS. It reached and passed the desktop layout assertion: titleWidth=372, titleHeight=99, scrollWidth=1024.
- `node scripts/check-cp1251-mojibake.mjs`
  - PASS: 0 suspicious tokens.
- `node scripts/check-js-syntax.mjs`
  - PASS: no staged JS or HTML files.
- `node --check` for the new/changed infra scripts
  - PASS.

## Review agent

Initial review failed because the Node UI guard used a different mojibake sentinel than the shell guard. Fixed `scripts/check-ui-architecture.mjs` to match the shell guard exactly.

Second review failed because the Node portable-path fallback did not normalize to repo root like the shell guard. Fixed `scripts/check-portable-paths.mjs` to resolve `git rev-parse --show-toplevel` and run `git grep` from that directory.

Final review PASS. The review agent verified:

- UI thresholds match shell and Node fallback: 465, 402, 0, 3.
- Mojibake sentinel matches in both guards.
- Portable path fallback scans from repo root.
- Linux/GitHub Actions path remains bash-first.
- `scripts/lib/find-chrome.mjs` has no absolute project path dependency.

## Remaining environment tails

The npm guard commands now work on this Windows runner without Git Bash in PATH. Existing CDP evidence PNGs for `BACK-055` and `BACK-069` were regenerated by smoke runs but are pre-existing dirty evidence files and are not part of this task commit.

## Commit

This report is included in the task commit `test(infra): add windows guard fallbacks`; the final pushed SHA was verified against `origin/feat/admin-tariff-api` after commit.
