# HOME-001 — Редизайн главного экрана (Сегодня) + упрощённая навигация

**Дата:** 2026-07-12
**Владелец:** Юрий (концепция) / Codex (реализация)
**Тип:** Product/UI
**Приоритет:** P1
**Статус:** Todo
**Рекомендуемая ветка:** `feat/home-dashboard-v2`

## Контекст

Юрий переработал главный экран и прислал два мокапа (светлая и тёмная тема) — сам мокап существует только как изображения в чате с Cowork, файлов в репозитории нет, поэтому спецификация ниже текстовая, максимально подробная. Если нужно свериться визуально — попросить Юрия прислать скриншоты повторно и положить их в `docs/tasks/assets/HOME-001-*`.

Это заменяет часть текущего главного экрана (`BACK-025` AI-дашборд) и напрямую закрывает `NEW-011` (задачи в общем контейнере) и `NEW-013` (перегруженный «Фокус дня»). Меняет и часть навигации, сделанной в `BACK-046`.

## Структура экрана «Сегодня»

Сверху вниз:

1. **Шапка:** аватар пользователя слева, заголовок «Сегодня ⌄» (дропдаун периода) по центру-слева, колокольчик уведомлений справа.
2. **Карточка «Фокус дня»:** крупный текст `N задач требуют внимания`, подпись `4 уже расставил приоритеты`, декоративная анимация (4 сферы логотипа). Кликабельна целиком.
3. **Заголовок секции:** `Задачи · текущий месяц`.
4. **4 метрик-карточки в ряд:** `Выполнено` (число + прирост, напр. `25 +4`), `Задачи` (число + дельта, напр. `14 ▲2 с вчера`), `Обещания` (число + просрочка, напр. `5 •1 просрочено`), `Прогресс` (процент + кольцо, напр. `57%`).
5. **Пронумерованный список приоритетных задач (1, 2, 3):** карточка на задачу с тегами `когда · направление · срочность` (напр. `Сегодня · Работа · скоро`), заголовком и короткой подписью (напр. кто ждёт/связь с релизом).
6. **Нижняя навигация (3 элемента):** `Сегодня` (home), центральная круглая кнопка с лого (4 сферы), `Календарь`.

## Карта переходов (обязательно к реализации)

| Где кликаем | Что происходит |
| --- | --- |
| Аватар пользователя (шапка) | Открыть экран «Профиль» |
| Колокольчик (шапка) | Открыть экран «Уведомления» |
| Карточка «Фокус дня» | Открыть быструю панель задач (popup/bottom-sheet с приоритетным списком, не полный экран статистики) |
| Метрик-карточка «Прогресс» | Открыть экран «Статистика» |
| Метрик-карточка «Задачи» | Открыть список задач без фильтра (все задачи, по приоритету) |
| Метрик-карточка «Обещания» | Открыть **тот же** список задач, но с фильтром «обещания» (см. открытый вопрос ниже) |
| Метрик-карточка «Выполнено» | Открыть тот же список задач с фильтром «выполнено за период» (по аналогии с «Задачи»/«Обещания» — не проговорено явно Юрием, реализовать по этому же паттерну и уточнить скриншотом при сомнении) |
| Пронумерованная карточка задачи (1/2/3) | Открыть карточку задачи (существующий task-detail) |
| Центр нижней навигации — **короткий тап** | Открыть AI-чат с «4» |
| Центр нижней навигации — **долгое нажатие** | Открыть голосовой ввод напрямую (минуя экран чата) |
| `Календарь` (нижняя навигация) | Существующий экран календаря |

Важно: `Задачи`, `Обещания` и `Выполнено` — это **один и тот же список/компонент**, различается только применённый фильтр. Не делать три отдельных экрана.

## «Обещание» — подтверждено Юрием 2026-07-12

Обещание — не отдельная сущность, а та же задача: фильтр `person != self` (заполнено поле «Человек/исполнитель», и это не сам Юрий). Использовать существующее поле, отдельный флаг/тип не нужен.

## Design tokens (оценка по скриншотам, не точные значения — сверить с Юрием при вёрстке)

Светлая тема: фон `#F1EEE4`, карточки белые с мягкой тенью, текст `#20241C`, вторичный текст `#7C7B72`, акцент зелёный `#7CB342` (тёмный вариант для текста на светлом — `#568203`).

Тёмная тема: фон почти чёрный с зелёным оттенком `#0C100B`, карточки `#161C14` (полупрозрачные, тонкая светлая обводка ~8% white), текст `#EDEFE6`, вторичный текст `#8B9086`, акцент зелёный ярче/со свечением `#9CCC65`–`#B6E26B`.

Радиусы: ~20-24px у крупной карточки фокуса, ~16px у метрик-карточек, круг у аватара и центральной кнопки нав-панели. Отступы: ~20-24px внутри крупной карточки, ~16px в метрик-карточках, ~12-16px между блоками.

## Что сделать

1. Собрать новую структуру экрана «Сегодня» по разделу «Структура экрана» выше, в светлой и тёмной теме (обе показаны в мокапах).
2. Реализовать карту переходов из таблицы выше.
3. Упростить нижнюю навигацию до 3 элементов; проверить, что уже сделанные `NEW-006` (safe area) и `NEW-008` (keyboard offset) не регрессируют на новой навигации.
4. Сделать единый компонент списка задач с параметром фильтра (`all` / `promises` / `done`), переиспользуемый из всех трёх метрик-карточек и из быстрой панели «Фокус дня» (для неё — фильтр по топ-приоритету, не весь список).
5. Долгое нажатие на центральную кнопку — стандартный `touch`/`pointer` long-press (500мс+), с визуальной обратной связью до срабатывания, чтобы не конфликтовать с обычным тапом на AI-чат.

## Не делать сейчас

- Не трогать сам экран AI-чата, Профиль, Уведомления, Статистику, Календарь изнутри — только точки входа/переходы к ним.
- Не переделывать data-модель задач ради «Обещаний», если действующее поле «Человек» уже подходит под фильтр (см. открытый вопрос).

## Проверка

1. Обе темы (светлая/тёмная) визуально соответствуют мокапам Юрия по структуре (не обязательно пиксель-в-пиксель).
2. Каждый переход из таблицы «Карта переходов» кликабелен и ведёт куда нужно.
3. Список задач с фильтрами `all/promises/done` — один компонент, не три копии.
4. Долгое нажатие на центр нав-панели не срабатывает случайно при обычном тапе (нет двойного открытия чата+голоса).
5. `NEW-006`/`NEW-008` фиксы не регрессируют на упрощённой навигации.
6. Пройти `check-ui-architecture.sh`, `check-cp1251-mojibake.mjs`, `check-js-syntax.mjs`.

## Git

Работать в `feat/home-dashboard-v2` (или продолжить в `feat/admin-tariff-api`, если Юрий предпочитает не плодить ветки — уточнить перед стартом). Не мерджить в `main` без подтверждения.
