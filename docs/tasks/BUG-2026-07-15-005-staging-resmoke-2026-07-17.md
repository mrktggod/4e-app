# BUG-2026-07-15-005 staging re-smoke — 2026-07-17

## Scope

Проверка старого BUG-005 / BACK-060 после reconciliation: доказать, что исходный exploit больше не создаёт задачу через unsigned sessionless bot-style request, и проверить соседние action-риски.

Target staging worker: `https://restless-lab-d737-staging.shelckograff.workers.dev`.

## Result

Status: passed for negative security paths.

Signed positive path: not re-run in Codex process, because `BOT_TOKEN` was not present in this local environment. Existing BACK-060 signed smoke remains the closing evidence for the signed happy-path: Юрий earlier confirmed signed `get-chat-members` returned `200 {"ok":true,"members":[]}` on staging.

## RAW smoke evidence

### Fresh linked-user exploit proof

- Registered fresh user: `bug005-linked-1784309751623@example.com`.
- Linked Telegram ID: `7910751623`.
- `POST /auth/link-telegram`: `200`, body included `"telegramId":"7910751623"` and `"accountMerged":false`.
- `GET /tasks` before exploit: `200 []`.

Unsigned exploit request:

`POST /`

Headers:

`x-action: save-task`

Body:

`{"task":{"direction":"incoming","type":"task","text":"BUG-005 linked unsigned save-task smoke 1784309751623"},"telegramUserId":"7910751623"}`

Response:

`STATUS: 403`

`BODY: {"ok":false,"error":"bot signature invalid"}`

Post-check:

- `GET /tasks` after exploit: `200 []`.
- `LINKED_UNSIGNED_SAVE_TASK_CREATED: False`.

### Additional unsigned bot-scoped task actions

`done-task` with `telegramUserId`:

- `STATUS: 403`
- `BODY: {"ok":false,"error":"bot signature invalid"}`

`delete-task` with `telegramUserId`:

- `STATUS: 403`
- `BODY: {"ok":false,"error":"bot signature invalid"}`

### Sibling risk checks

`update-task` with bot-style `telegramUserId`, unsigned/sessionless:

- `STATUS: 401`
- `BODY: {"ok":false,"error":"Не авторизован"}`
- Effect: no unsigned bot-style update path observed.

`set-reminder` with bot-style `telegramUserId`, unsigned/sessionless:

- `STATUS: 401`
- `BODY: {"ok":false,"error":"Не авторизован"}`
- Effect: no unsigned bot-style reminder path observed.

## Code alignment

Current worker route flow matches the live result:

- `save-task`, `done-task` and `delete-task` go through `readVerifiedActionBody()`.
- `shouldRequireBotSignature()` requires bot HMAC for bot-scoped shared task actions when `telegramUserId`, `assigneeTgId`, `task.assigneeTgId`, `chatId=user_*` or `chatId=group_*` is present.
- `update-task` and `set-reminder` also use `readVerifiedActionBody()`, but then require a normal session and return `401` without one.

## Encoding guard

`pm/bugs.md` was restored from CP1251 mojibake using `node scripts/check-cp1251-mojibake.mjs --fix`.

Checker after fix:

`CP1251 mojibake check passed: 0 suspicious tokens`.
