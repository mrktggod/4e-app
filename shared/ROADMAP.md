# Roadmap — 4 AI-секретарь

Единая дорожная карта продукта, стратегии, технических приоритетов и PM/QA-планирования.

**Правило:** в проекте должен быть только один roadmap-файл — `shared/ROADMAP.md`. Не создавать второй roadmap в `pm/` или корне репозитория. При стратегических решениях Юрия, изменениях в `DEVELOPMENT_LOG.md` или обновлениях backlog синхронизировать этот файл и `pm/backlog.md`.

## Vision

Персональный операционный директор — партнёр, который следит за всем, напоминает, анализирует и берёт рутину на себя.

## North Star

Пользователь открывает 4 утром и за 30 секунд видит что важно сегодня, что горит и что ждёт команда.

## Продуктовые столпы

| # | Столп | Суть |
| --- | --- | --- |
| 1 | AI-советник | Лёгкий доступ к анализу и советам в любой момент |
| 2 | Дашборд жизни | Всё важное в одном месте, собирается само |
| 3 | Автоматизация | 4 изучает привычки и сам предлагает шаблоны |
| 4 | Омниканальность | Единая база на всех платформах |

## Принципы развития

1. **Mobile-first, всегда** — основная аудитория в Telegram и VK на телефоне.
2. **Нулевой барьер входа** — Mini App, без установки отдельного приложения.
3. **Данные пользователя — его** — прозрачность в том, что хранится и как этим управлять.
4. **Автоматизация без навязчивости** — AI предлагает, человек решает.
5. **Омниканальность = единая база** — один профиль, все платформы синхронизированы.

---

## Now — Горизонт 0 (июнь–июль 2026)

Цель: устранить критические технические проблемы, завершить редизайн, подготовить к первым внешним пользователям.

| Направление | Задачи | Приоритет | Статус |
| --- | --- | --- | --- |
| Технический долг | Email через Resend, сброс пароля (бэкенд), тестовый платёж | P0 | Done |
| Редизайн | Фаза 9 — биометрическое согласие 152-ФЗ | P1 | Done |
| Единая модель пользователя | VK + TG + Email в одном профиле | P1 | Done |
| Миграция хранилища | KV → D1 | P2 | Done |
| Продакшн email | Домен 4-ai.site верифицирован в Resend, FROM: noreply@4-ai.site | P1 | Done |
| AI-планерный дашборд | BACK-025 — утренний AI-дашборд в стиле "План на сегодня"; BACK-055 — уведомления как лента внимания; BACK-056 — убрать отрицательное время после 22:00 | P1 | BACK-025 Done; BACK-055 Todo; BACK-056 Todo |
| Качество задач | Фаза 11 — относительные даты в карточках задач; BACK-019 — улучшенные карточки в списке; BACK-022 — ручной MVP детальной карточки задачи; BACK-044 — упрощение детальной карточки | P1 | BACK-022 Done; BACK-044 Done; BACK-019 Ready for QA |
| Доступ и авторизация | BACK-024 — Telegram вход не должен упираться в тупиковую подсказку между Mini App и ботом; BACK-036 — web fallback Telegram-входа без `tg://resolve`; BACK-041 — bot-side возврат на сайт из `/start auth_*` | P1 | BACK-024 Done; BACK-036 Ready for QA; BACK-041 Ready for QA |
| Голосовой ввод | BACK-021 — MediaRecorder + Whisper вместо SpeechRecognition (фикс BUG-2026-06-29-002, iOS) | P1 | Triaged |
| 152-ФЗ | BACK-007 — номер уведомления РКН вшит в privacy.html, ссылка видна на login/register/онбординге | P1 | Ready for QA |
| Staging-контур | BACK-034 — Staging D1/KV + `wrangler --env staging`, тестовый бот, dev-версия Pages подняты; staging AI smoke пройден | P1 | Ready for QA |
| Хостинг фронта | INFRA-001 — whitelist-фронт на Workers Static Assets + маршрут `app.4-ai.site/*` через Worker; контрольная точка: проверка из РФ-сети без VPN | P0 | In Progress |
| VK-хостинг | INFRA-004 — VK-поверхность переехала на хостинг VK Mini Apps; API остаётся на `edge.4-ai.site`, потому что Workers доступны из РФ | P1 | Done |
| VK на мобильном интернете РФ | INFRA-005 — промежуточный RU API proxy в Yandex Cloud для VK Mini App; шаг 1 = gateway-прокси на `edge.4-ai.site` + VK-сборка с новым API base, без полного переноса backend | P0 | In Progress |
| UI-архитектурный guard | BACK-049 — правило LESS + BEM для нового UI-кода, запрет роста inline `style`/handlers, проверка в pre-commit и GitHub Actions | P1 | Done |
| Accessibility baseline | BACK-050 — базовая доступность критических сценариев: auth-формы, статусные сообщения, keyboard/focus, dialog-поведение quick-add/contact/consent и touch-target smoke перед закрытым тестом | P1 | Ready for QA — code baseline готов, нужен ручной keyboard/mobile smoke |
| Единая рабочая копия | Одна рабочая папка, обмен только через git (убрать расхождение Documents/Desktop) | P1 | Todo |
| Закрытый тест и измерение | BACK-035 — QA smoke; BACK-048 — dev/test аккаунты; BETA-001 — закрытый тест 5-10 пользователей; ANALYTICS-001 — lite-события активации; FEEDBACK-001 — быстрый feedback loop | P1 | Todo |

---

## Next — Горизонт 0.5 + 0.8 (июль–август 2026)

Цель: закрыть правовые риски до публичного промо, привести кодовую базу в порядок.

| Направление | Задачи | Приоритет | Статус |
| --- | --- | --- | --- |
| 152-ФЗ | Уведомление РКН подано, номер получен (2026-07). Осталось: номер в privacy.html, ссылка на privacy в регистрации | P1 | Частично Done |
| AI-память v1 | Профиль опыта пользователя: SMART-006 — профиль в контексте AI-чата (имя, статистика, люди); SMART-007 — память фактов между сессиями (D1 user_facts + экран «Что 4 знает обо мне»). Детали: `ЗАДАЧИ_УМНЫЙ_АССИСТЕНТ.md` | P1 | SMART-007 Done (2026-07-06); SMART-006 Ready for QA — staging AI smoke пройден 2026-07-07 |
| Календарный фундамент | CAL-001 — концепция календаря 4: как задачи, уведомления, AI-дашборд, AI-память и внешняя занятость работают как единый "штаб дня"; этапы CAL-002/CAL-003; privacy model для внешних календарей | P1 | Todo — сначала концепция, не разработка CAL-002 |
| Умный групповой секретарь | SMART-001 — ростер участников чата; SMART-002 — исполнитель с TG ID в задаче (mention/reply/fuzzy-матч); SMART-003 — кнопка «Написать» исполнителю; SMART-004 — лаконичная фиксация в группах (однострочное подтверждение) | P1 | SMART-003 Done; SMART-001/002/004 Ready for QA |
| Проактивность бота | SMART-005 — утренняя сводка по чату (подключить готовый checkBriefings + Cloudflare Cron вместо setInterval); SMART-011 — умный пинг «Жду от людей» (кнопка «Напомнить N», требует SMART-002) | P2 | SMART-011 In Progress (2026-07-06); SMART-005 Todo |
| Архитектура ядра | ARCH-001 — распил index.html: CSS/JS в модули, общее ядро + тонкие адаптеры TG/VK (готовит почву для MAX и сторов), убирает ~70% дублирования с vk.html | P1 | In Progress — platform layer `scripts/platform-adapter.js` уже держит Telegram/VK/web detection, Telegram auth URL helpers, OAuth PKCE/state helpers, referral helpers, dialog/focus helpers, form-error helpers, email-validator, password-toggle helpers, Enter-submit helper, VK launch params и VK Mini Apps adapter; новые экраны SMART-003/007 уже написаны на LESS/BEM классах вместо inline |
| Слияние аккаунтов | BACK-026 — поиск существующего профиля по telegramId/vkId/email, перенос задач (механика существовала раньше, подтверждена ревью). BACK-051 — тост-уведомление после слияния. Предпосылка рефералок | P1 | BACK-026 Done (2026-07-06); BACK-051 Done |
| Авторизация через сервисы РФ | BACK-045 — VK ID + Яндекс ID OAuth готовы и задеплоены на staging; ждут реальных client_id/secret от Юрия для live smoke | P1 | Ready for QA |
| Активация | ONBOARD-001 — первый AI-план за 60 секунд: пользователь создаёт 3 задачи и сразу видит полезный план дня | P1 | Todo |
| CI и смоук-тесты | BACK-037 — GitHub Actions: проверка кодировки, линт, минификация; `api-smoke` покрывает auth/tasks CRUD/transcribe, staging smoke пройден | P2 | Ready for QA |
| Аналитика | BACK-038 — события (вход, задача, шеринг) + D1/D7 retention; `/analytics/summary` закрыт `ADMIN_SECRET`, убран из личного экрана статистики | P2 | Ready for QA |
| Данные в России | Перенос ПД в Yandex Cloud PostgreSQL | P1 | Manual blocker — ждёт Yandex Cloud PostgreSQL от Алексея |
| Premium trust positioning | Обсудить с Claude Юры, как безопасность, приватность и контроль данных становятся частью дорогого позиционирования 4 | P1 | Ready for discussion |
| CSS-архитектура | LESS + BEM + минификация | P2 | Partial Done — LESS/minification есть; BEM и очистка legacy inline-кода идут через BACK-049 и поэкранные правки |
| Семантический HTML | `<main>`, `<header>`, `<nav>`, aria-label | P3 | Done |
| PostgreSQL prep | Подготовить код к будущему PostgreSQL без live credentials | P2 | Done — adapter merged in worker commit `37f9dda` |

---

## Later — Горизонт 1 (август–сентябрь 2026)

Цель: запустить монетизацию и вирусные механики.

| Направление | Задачи |
| --- | --- |
| Монетизация | VK Pay готов к VK live QA; Telegram Stars готов к TG live QA; дальше ЮKassa, командный Workspace (5/10/25 чел.); ценовую модель вынести в отдельное решение |
| Вирусные механики | 1) Шеринг-карточка «План дня / Итоги дня» (glass-стиль, водяной знак) в VK Stories и TG; 2) реферальная ссылка +30 дней обоим (после BACK-026); 3) онбординг с wow-моментом — первый AI-план за 60 сек; 4) streak «дней с планом» + 2–3 достижения в шеринг-карточке; 5) еженедельная AI-сводка «Твоя неделя» (мини-Wrapped) |
| AI-персонаж | Сдержанная версия персонажа AI-блогера внутри приложения (аватар, тон сводок) — модель Duolingo: узнаваемость между VK Клипами и продуктом. Требует character bible (Юрий) |
| AI-память v2 | Обучение на поведении: завершённые/перенесённые задачи, время продуктивности → персонализированный план и порядок «Начать с»; SMART-012 — адаптивное время напоминаний |
| Календарный MVP | CAL-002 — дневная лента внутри 4: задачи + события + свободные окна; длительность и плановое время у задач; ручной time blocking; AI-план с учётом занятости. CAL-003 остаётся после утверждения privacy-модели и первых сигналов доверия |
| Умный чат: действия | SMART-008 — действия из чата (перенести/закрыть/изменить/показать, с подтверждением); SMART-009 — один уточняющий вопрос с кнопками; SMART-011 — умный пинг «Жду от людей» (напомнить исполнителю за пользователя); SMART-010 — дедупликация задач |
| «Задача — это промпт» | SMART-013 — AI-декомпозиция задачи на этапы (чек-лист) + углубление в шаги через чат задачи (BACK-032); кандидат на wow-момент онбординга и основу вирального ролика 2Б. Идея Юрия 2026-07-04 |
| Омниканальные поверхности | OMNI-001 — Web/PWA для desktop-планирования, виджеты, share sheet, email/browser как источники задач, native voice. Каналы не меняют сущность продукта: 4 остаётся личным штабом дня, а не ботом или корпоративным workspace |
| Канал MAX | Мини-апп + бот в мессенджере MAX (dev.max.ru): тот же веб-стек + MAX Bridge, четвёртый адаптер к общему ядру. Требует верифицированный профиль ИП/самозанятого на платформе MAX (Юрий) и модерацию |
| Сторы: RuStore | Публикация веб-обёртки (PWA) в RuStore — самый низкий барьер, домен 4-ai.site уже есть. Требует standalone-режим без TG/VK SDK (после распила ядра) |
| Качество | BACK-023 — расширение детальной карточки задачи после MVP; push-уведомления через Telegram-бот, BACK-057 — Offline Free Mode (см. ниже), улучшение распознавания голоса |

---

## Future — Горизонт 2 (октябрь–декабрь 2026)

Цель: реализовать столп 3 — автоматизация и предиктивность.

| Направление | Задачи |
| --- | --- |
| AI-поведение | AI-память v3: анализ паттернов ("ты каждый понедельник ставишь standup, хочешь автоматически?"); предиктивные напоминания; авто-шаблоны; расширение AI-сводки после BACK-025 |
| Сторы: Google Play, App Store | Google Play через TWA (обёртка над PWA), затем App Store через нативную обёртку (Capacitor) — Apple строже к webview-приложениям, потребует нативных элементов и учёта правил платежей |
| Дашборд жизни | Недельный/месячный отчёт по продуктивности с AI-анализом |
| Команды | Командный дашборд, делегирование с трекингом, комментарии в задаче |
| Интеграции | Google Calendar (двусторонняя синхронизация), Notion (импорт задач), Webhook API |

---

## Platform — Горизонт 3 (2027+)

Цель: реализовать столп 4 — настоящая омниканальность и платформа.

| Направление | Задачи |
| --- | --- |
| Каналы | WhatsApp Business API, Instagram DM, Email-парсинг (Gmail / Яндекс Почта) |
| Приложения | Полноценные нативные iOS/Android (если обёртки упрутся в ограничения), десктоп |
| Нативные суперспособности (после сторов) | NATIVE-001 календарь (двусторонняя синхронизация + AI учитывает занятость); NATIVE-002 push с действиями «Готово/+1 час»; NATIVE-003 голос через Siri Shortcuts/Google Assistant — задача без открытия приложения; NATIVE-004 виджеты + share sheet; NATIVE-005 гео-напоминания. Принцип: эффект «Алисы» легальными средствами платформ; сбор данных — только opt-in по одному источнику, отражается в «Что 4 знает обо мне» (идея Юрия 2026-07-04) |
| Платформа | Marketplace интеграций, открытый API для разработчиков, white-label для бизнеса |
| AI-эволюция | Голосовой режим 24/7, Proactive mode — 4 сам инициирует общение, мультиагентность |

---

## Технические пороги масштабирования (без срока — только по метрике)

Не привязано к горизонту/дате. Делать, когда реально достигнут порог, не раньше — не тратить время на преждевременную оптимизацию.

| Порог | Что сломается | Где в коде | Что делать | Статус |
| --- | --- | --- | --- | --- |
| Ориентировочно 1000–2500 суммарных зарегистрированных пользователей с включёнными напоминаниями/утренним брифингом (после этого — «когда будет больше 2000 онлайна») | Крон-цикл `collectBriefingsForNow`/reminders в `4e-worker/worker.js` идёт `for (const userId of userIds)` последовательно по всем пользователям без батчинга (~4-5 KV/D1 подзапросов на каждого) — упрётся в лимит Workers «10 000 подзапросов за один вызов» (Workers Paid) и начнёт не долетать до части пользователей за один прогон крона | `4e-worker/worker.js`, функция `collectBriefingsForNow` (используется `runBriefingsCron`/`scheduled()`) | Батчинг чанками через `Promise.all`, либо перенос обхода пользователей на Cloudflare Queues/Durable Objects вместо линейного цикла в одном cron-вызове | Не начато, анализ 2026-07-06 |

## Решения

| Дата | Решение | Контекст |
| --- | --- | --- |
| 2026-06-20 | Полное соответствие 152-ФЗ — перед публичным промо, не сейчас | Сейчас внутренние тесты, риски минимальны |
| 2026-06-20 | Редизайн санкционирован | Все 9 фаз патчей, Codex устанавливает по одной |
| 2026-06-25 | Миграция на Yandex Cloud — перед монетизацией | PostgreSQL b2.medium ~3 430 ₽/мес |
| 2026-06-27 | Один roadmap-файл | `pm/roadmap.md` объединён с `shared/ROADMAP.md`; единственный актуальный roadmap теперь `shared/ROADMAP.md` |
| 2026-06-27 | Продакшн email домен | 4-ai.site верифицирован в Resend через Cloudflare Auto configure; FROM адрес обновлён на noreply@4-ai.site |
| 2026-06-27 | Новое позиционирование | Отстройка от "таск-менеджер" → "штаб на каждый день"; конкретные цены не фиксировать до отдельного обсуждения монетизации |
| 2026-06-27 | Цены отложены | Ценовая модель не является условием текущего merge roadmap |
| 2026-07-04 | РКН: уведомление подано, номер получен | Осталось вшить номер в privacy.html и добавить ссылку на privacy в регистрацию |
| 2026-07-04 | «Чистая/грязная» версия — через ветки и staging, не через две копии кода | main = чистая (пользователи), dev + staging D1/KV + тестовый бот = грязная; вторая копия кода ведёт к расхождению |
| 2026-07-04 | AI-память — приоритетное направление | Накопление и полезное использование опыта пользователя в ответах AI; поэтапно: v1 профиль (Горизонт 0.5–0.8) → v2 обучение на поведении (Горизонт 1) → v3 предиктивность (Горизонт 2) |
| 2026-07-04 | Платформенная экспансия | Порядок: MAX мини-апп → RuStore → Google Play → App Store. Предпосылка для всех — распил монолита на ядро + адаптеры |
| 2026-07-05 | Итоговая схема хостинга | Telegram Mini App остаётся на GitHub Pages; `app.4-ai.site` — глобальный домен и фронтовый контур для мира, сторов и сетей с VPN; VK-поверхность перенесена на хостинг VK Mini Apps и подтверждена phone-smoke без VPN; `edge.4-ai.site` остаётся единым API и доступен из РФ. Workers Static Assets используем как глобальный контур, но не считаем способом победить жёсткие белые списки в РФ |
| 2026-07-05 | Авторизация через сервисы РФ | Для РФ-аудитории добавить вход через VK ID и Яндекс ID как доверенные локальные провайдеры. Это усиление доступности и доверия, а не замена Email/TG; реализация должна учитывать единую модель аккаунта и слияние дублей |
| 2026-07-06 | UI-архитектурное правило | Новый UI-код пишем по схеме HTML = структура, LESS = стили, JS = поведение; BEM-подход для новых классов; старый inline-долг не переписываем массово, но запрещаем его рост guard-скриптом |
| 2026-07-06 | Accessibility baseline перед закрытым тестом | Доступность фиксируем как часть качества критических сценариев, а не как отдельную декоративную полировку: вход, формы, статусные сообщения, фокус, модалки и размеры целей нажатия должны проверяться в QA |
| 2026-07-06 | Accessibility как постоянное UI-правило | Для нового и изменяемого UI доступность входит в Definition of Done: labels/accessible names, видимый focus, keyboard flow, доступные status/error сообщения, dialog-поведение и mobile touch targets |
| 2026-07-06 | Git-синк: fetch в начале сессии, push+проверка в конце | Инцидент 2026-07-06: Codex (Алексей) начал сессию на `feat/admin-tariff-api`, не зная о работе, уже сделанной параллельно на той же ветке — из-за этого чуть не возникла путаница с нумерацией BACK-050/051. Разошлось без потерь (чистый merge `eda8246`), но правило закрепляем: каждый Codex (и Юрин, и Алексея) делает `git fetch origin` первым действием сессии, и проверяет `git rev-parse HEAD` == `git rev-parse origin/<ветка>` после push, а не просто верит, что «запушил» |
| 2026-07-06 | Публичность репозиториев — оставить как есть, пока не решено иначе | `4e-app` (код фронта, `pm/backlog.md`, `shared/ROADMAP.md`) — Public, обязателен пока фронт на GitHub Pages Free (см. INFRA-003). `4e-worker` (боевой backend-код) — Private. Живых секретов в открытом коде нет (только PLACEHOLDER, реальные ключи в GitHub Secrets). Проанализировали альтернативы (отдельный private-репо для pm/shared, свой git на Yandex Cloud, Google/Яндекс Диск, Notion) — решение не принято, вопрос открыт |
| 2026-07-07 | ⚠️ ПОД ВОПРОСОМ (частично снято 2026-07-08, см. ниже): гипотеза «`edge.4-ai.site` доступен из РФ без VPN» (см. решение 2026-07-05) | Живой тест Юрия: `https://app.4-ai.site/` И `https://edge.4-ai.site/` оба не открылись на мобильном LTE без VPN («не смог подключиться к серверу»), хотя из внешней (не-РФ) сети оба отдают корректный ответ. Проверена только ОДНА точка/оператор — по правилу INFRA-002 нужна вторая независимая точка/оператор (Wi-Fi или другой оператор) прежде чем менять архитектурные выводы |
| 2026-07-08 | Вторая точка РФ-теста: `edge.4-ai.site` подтверждён доступным без VPN, но `app.4-ai.site` — НЕТ (вероятно RF-блокировка конкретного домена, не баг конфига) | Юрий проверил с другого Wi-Fi (не домашний), без VPN: `edge.4-ai.site` отдал корректный ответ, VK-поверхность полностью рабочая — риск для INFRA-004 снят. `4-ai-app.pages.dev` пустой — ожидаемо, старый Pages-путь. НО `https://app.4-ai.site/` — чёрный экран, ничего не грузится, на той же сети, где `edge.4-ai.site` только что отработал нормально. Это вторая независимая точка (после LTE), где именно `app.4-ai.site` не грузится. Проверено дополнительно: (1) `wrangler.toml` в `.tmp-4e-app-publish` корректно настроен на Workers Static Assets (`routes` на `app.4-ai.site`/`app.4-ai.site/*`, `zone_name = "4-ai.site"`, `[assets] binding = "ASSETS"`) — конфиг НЕ указывает на Pages; (2) прямой запрос на `https://app.4-ai.site/` извне РФ (не-РФ сеть) отдал полностью корректную страницу с правильным содержимым. Значит сервер/деплой в порядке, а проблема — именно в РФ-сети: скорее всего таргетированная блокировка конкретного домена (SNI/DNS-уровень), а не общая блокировка Cloudflare Workers или ошибка конфигурации. `edge.4-ai.site` на той же инфраструктуре не блокируется — расхождение по конкретному хосту типично для точечной блокировки Роскомнадзора по домену, а не по IP/провайдеру. НЕ переводить INFRA-001 в Done; вопрос теперь стратегический (нужен ли отдельный/другой домен для фронта), а не технический баг для правки Кодексом |
| 2026-07-07 | РФ web-провайдер оплаты — оставить оба, не выбирать один | Юрий подтвердил: CloudPayments (уже в коде) остаётся, ЮKassa добавляется вторым провайдером; VK Pay и Telegram Stars уже реализованы отдельными каналами. Единый billing layer (предложение Алексея) должен уметь работать поверх нескольких провайдеров, а не переключать один на другой. Открыто: фискализация чеков 54-ФЗ по каждому провайдеру, зарубежный контур (Merchant of Record), стратегия для iOS (PWA-companion vs IAP) — ждут отдельного решения Юрия |
| 2026-07-07 | Сторонние дизайн-skills — не подключать напрямую | Согласились с выводом Алексея: делать свой `4e-ui-design` skill поверх текущих правил (`docs/ui-architecture-rules.md`, `AGENTS.md`, `pm/qa-checklist.md`) вместо прямого подключения стороннего дизайн-skill. Причина: сторонний skill может генерировать inline style/onclick или менять сценарий/монетизацию в обход approval-gate |
| 2026-07-08 | Доп. поля профиля из OAuth (email/телефон/дата рождения) — после закрытого теста, задача `BACK-058` | Проверено по официальной документации: Telegram не даёт ни email, ни телефон (кроме отдельного «Поделиться контактом» в боте), ни дату рождения никогда. VK ID даёт email/phone по отдельным scope с согласием пользователя, но дату рождения и пол новая VK ID не отдаёт вообще (было в старом API, убрано). Яндекс ID — единственный источник даты рождения, плюс email/phone, каждое по отдельному scope с согласием. Решение: не запрашивать scope «про запас», добавлять только под конкретную фичу, каждое поле — отдельная строка в согласии (152-ФЗ) |
| 2026-07-08 | Позиционирование: омниканальный штаб дня | 4 не позиционируется как "бот в мессенджере" или generic task manager. Мессенджеры — быстрый канал привычки; продуктовая сущность — личный штаб дня на разных поверхностях: mobile, Web/PWA, календарь, уведомления, виджеты, share sheet, voice, email/browser sources |
| 2026-07-08 | Календарь поднят в ближайшую продуктовую проработку | Календарь становится связующим слоем задач, уведомлений, AI-дашборда и AI-памяти. Сначала CAL-001 как концепция и privacy-модель, затем внутренний календарный MVP, затем read-only внешняя синхронизация и только после доверия — двусторонняя запись |
| 2026-07-08 | Roadmap фильтруется через beta gate и монетизацию | Now — только то, что помогает первым активным пользователям понять ценность, пройти вход, дать обратную связь и не упереться в сломанные сценарии. Later/Future не удаляются, но не забирают командное время до первых сигналов удержания и готовности платить |
| 2026-07-12 | ⚠️ Авто-деплой `4e-worker` на `main` — последние 4 запуска упали (failure) | При диагностике `NEW-018` обнаружено: GitHub Actions `Deploy Worker` (`.github/workflows/deploy.yml`, триггер `push` на `main`) — последние 4 прогона (2026-07-04/05) все завершились `failure`. Push в `feat/admin-tariff-api` эту автоматизацию вообще не запускает (только `main`), поэтому пока ветка не смержена, это не блокирует текущую работу — но если и когда `feat/admin-tariff-api` смержат в `main`, автодеплой по факту, скорее всего, тоже упадёт, если причину не разобрать заранее. Нужно отдельно проверить логи этих 4 упавших ранов перед любым мерджем в `main` |

## Пожелания / ожидают решения

| Дата | Тема | Пожелание | Статус |
| --- | --- | --- | --- |
| 2026-06-28 | Git-процесс: разбор веток и merge в `main` | Рабочая гипотеза: разбирать готовые ветки 1 раз в неделю + срочно для P0/P1. GitHub Desktop оставить как удобный вариант для Алексея, но не делать его обязательным правилом для Юрия. Рискованные `push`, `merge`, `pull --rebase`, force push и reset согласовывать отдельно. | Дополнено 2026-07-06 конкретным правилом fetch/push-проверки (см. решения выше); финальное правило по-прежнему утверждает Алексей |
| 2026-07-06 | Приватность PM-документов (`pm/backlog.md`, `shared/ROADMAP.md`) | Сейчас публичны вместе с `4e-app`. Варианты: отдельный private-репо на GitHub (проще всего, но данные в США); свой git на Yandex Cloud (в русле RU-инфраструктуры, но новый сервис на поддержке); Google/Яндекс Диск или Notion — не git, ломают текущий процесс диффов и проверки коммитов ботами | Ждёт решения Юрия / Алексея |

## Не делать

| Что | Почему |
| --- | --- |
| Generic PM-roadmap про “управление проектом” | Продукт — персональный AI-секретарь, а не инструмент для ведения разработки |
| Новые фичи Горизонта 1 до закрытия P0/P1 Горизонта 0 | Сначала стабильность, auth/email/payments и 152-ФЗ |
| Большой рефакторинг CSS до завершения редизайна и правовых задач | Фундамент запланирован на Горизонт 0.8 |
| Наращивать inline UI-код в `index.html`/`vk.html` | Это ухудшает кеширование, скорость правок и поддержку; новый UI должен идти через LESS + JS |
| Разгонять новые каналы до beta gate | MAX, RuStore, native, виджеты и email/browser sources усиливают продукт позже, но до первых активных пользователей важнее доказать ежедневную ценность "штаба дня" |
